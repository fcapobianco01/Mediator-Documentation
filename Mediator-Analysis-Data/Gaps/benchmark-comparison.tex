\begin{table*}
    \centering
    \begin{tabular}{r|cc|cccccc|c|ccc|}
    
    &
    \rotfortyfive{total hooks} &
    \rotfortyfive{hooks analyzed} &
    \rotfortyfive{sub $\rightarrow$ obj} &
    \rotfortyfive{sub $\rightarrow$ op}  &
    \rotfortyfive{obj $\rightarrow$ sub} &
    \rotfortyfive{obj $\rightarrow$ op}  &
    \rotfortyfive{op  $\rightarrow$ sub} &
    \rotfortyfive{op  $\rightarrow$ obj} &
    \rotfortyfive{dynamic $\rightarrow$ static} &
    \rotfortyfive{input $\rightarrow$ mediator} &
    \rotfortyfive{external $\rightarrow$ input} &
    \rotfortyfive{external $\rightarrow$ mediator} \\ \hline
    
    
SELinux    & 173 & 102 &  270/56 &     4/0 &  202/18 &     6/3 &    13/0 &    16/0 &   18/11 & 537/235 &     0/0 & 348/172 \\
Tomoyo     & 28  & 23  &    63/0 &    45/0 &    61/0 &    93/0 &    41/0 &    87/0 &  192/27 &  379/61 &     0/0 &  122/27 \\
AppArmor   & 34  & 20  &    58/0 &     0/0 &    36/6 &     0/0 &     2/0 &     3/0 &     2/2 &  105/52 &     0/0 &   66/29 \\ \hline
    \end{tabular}
    \caption[\label{tab:infoflow-baseline-current-comparison}]%
    {Comparison of positive counts by dimension between different configurations of the information-flow analysis. \par {\footnotesize \textit{For each LSM, the baseline disables all improvements discussed in \ref{sec:ifc}, while the field-sensitive row enables only field-sensitivity. The strict noninterference row is same as the field-sensitive row with the addition of implicit flows and constant tainting to capture uncaught true positives. The last row applies relaxed noninterference to reduce false-positive rate. The baseline is being compared with only field-sensitivity enabled for a fair comparison because it 1) doesn't support constant labeling and 2) has implementation bugs which makes it incapable of capturing implicit flows. The last two rows shows the effectiveness of relaxed noninterference in reducing false-positive rates.}}}
\end{table*}